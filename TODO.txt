

- document functions

- error return to parse params
- error return to parse block stmt


ERRORS:
- sort this out %!v(PANIC=runtime error: invalid memory address or nil pointer dereference)
                %!v(PANIC=runtime error: invalid memory address or nil pointer dereference)
- if you see an error within a function, just exit the function and continue
- print(1,2,3,); is valid!
- 3 < 2 < 3 is valid up until semantic analysis!


- set up go mod
- x[0] = 11;

